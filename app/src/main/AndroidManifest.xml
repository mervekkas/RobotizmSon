<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robotizm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".testler.IkinciTest_Activity"></activity>
        <activity android:name=".testler.BirinciTest_Activity" />
        <activity android:name=".MiniTestActivity" />
        <activity android:name=".SoruIsaretiActivity" />
        <activity android:name=".sifreUnuttumActivity" />
        <activity android:name=".uyeOlButtonActivity" />
        <activity android:name=".AnaSayfaActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>